package lessons.lesson_13.classWork;

//Возвращаемые методы
public class Task_2 {
    public static void main(String[] args) {
       // int x = sum(1, 2, 3);
       // int y = sum(1, 4, 9);
       // int finalResult = x + y;
       // System.out.println(finalResult);
    }

         int sum(int a, int b, int c) {
        return a + b + c;
    }
//    static int sum(int a, int b, int c) {
//        byte sum = (byte) (a + b + c);// Можно меньше чем int //long sum нельзя
//        return a + b + c;
//    }
}
